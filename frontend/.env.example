# Frontend environment variables — copy to `.env` and adjust.
# `.env` is gitignored; never commit real values.

# Base URL of the backend API. Empty string means same-origin (recommended
# in production behind Nginx reverse proxy). For local dev with a separate
# backend port, set e.g. http://localhost:8080
VITE_API_BASE_URL=
